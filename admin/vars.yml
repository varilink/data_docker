# ------------------------------------------------------------------------------
# admin/vars.yml
# ------------------------------------------------------------------------------

# Variables for the creation of the admin service's settings.py file from a
# Jinja template. We use this approach so that the template can shared between
# Docker Compose and Ansible deployments.

---

admin_secret_key: "\
  django-insecure-zq7@^1_x5-d)o^ea0dr^3n+s+e@3ob0jx9)k)x&dz!6)sfsfft"
admin_allowed_hosts: ["'admin'"]
admin_csrf_trusted_origins: ["'http://admin'"]
admin_static_url: /static/
server_name: data
admin_server_name: admin
